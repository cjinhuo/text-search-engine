"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getReleaseLine = getReleaseLine;
exports.getDependencyReleaseLine = getDependencyReleaseLine;
const helper_1 = require("./helper");
function getReleaseLine(newChangesetWithCommit_1, _bumpType_1, _option_1) {
    return __awaiter(this, arguments, void 0, function* (newChangesetWithCommit, _bumpType, _option, isNestedFormat = false) {
        if (!newChangesetWithCommit.commit) {
            throw new Error('CommitId Not Found From Changeset');
        }
        const { email, author, date, intactHash } = yield (0, helper_1.getInfoByCommitId)(newChangesetWithCommit.commit);
        const remoteUrl = yield (0, helper_1.getGitRemoteUrl)();
        const commitId = (0, helper_1.formatGitCommitId)(newChangesetWithCommit.commit);
        const lines = (0, helper_1.splitSummary)(newChangesetWithCommit.summary);
        const result = lines.map((line) => {
            return `${isNestedFormat ? '  ' : ''}- ${line} @${author || email} · ${date} · [#${commitId}](${remoteUrl}/commit/${intactHash})`;
        });
        return result.join('\n');
    });
}
function getDependencyReleaseLine(newChangesetWithCommits, _dependenciesUpdated) {
    return __awaiter(this, void 0, void 0, function* () {
        if (!newChangesetWithCommits.length)
            return '';
        const releaseLines = [];
        for (const changeset of newChangesetWithCommits) {
            const releaseLine = yield getReleaseLine(changeset, '', '', true);
            releaseLines.push(releaseLine);
        }
        const dependenciesUpdated = _dependenciesUpdated[0];
        const updatedBy = `- Updated By ${dependenciesUpdated.name}: ${dependenciesUpdated.oldVersion}->${dependenciesUpdated.newVersion}`;
        return `${updatedBy}\n${releaseLines.join('\n')}`;
    });
}
