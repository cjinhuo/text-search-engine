import { Command } from '../Command';
/**
 * A command that prints the clipanion tokens.
 */
export declare class TokensCommand extends Command<any> {
    static paths: string[][];
    args: string[];
    execute(): Promise<void>;
}
